import org.example.*;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class HelloMockitoTest {

    /*
    PersonDB ga;
    TranslationServiceClass xd;
    final private HelloMockito helloMockito = new HelloMockito(ga, xd);

    @Test
    void greetPerson() {
        String greeting = helloMockito.greet(1, "World", "");
        assertEquals("Hello, World, from Mockito!", greeting);
    }
    */
}

